import React from 'react';
import App from './state_07';
import { createRoot } from 'react-dom/client';
import { ReactDOM  } from 'react';

const root = createRoot(document.getElementById("root"));

root.render(
    <React.StrictMode><App /></React.StrictMode>);


